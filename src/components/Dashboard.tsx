import React from 'react';

// This component is no longer needed as FIMS will have its own dedicated dashboard
// The main dashboard will be handled by the parent website
export const Dashboard: React.FC = () => {
  return null;
};